#include "BinaryTree.h"

BinaryTree::BinaryTree() {
	root = new Node();
	root->value = 0;
	root->left = nullptr;
	root->right = nullptr;
}

BinaryTree::~BinaryTree() {
	//destructor
}

bool BinaryTree::search(int passedVal) {
	//implement so insert and remove can use this function
	return true;
}

bool BinaryTree::insert(int passedValue) {
	//search first
	return true;
}

bool BinaryTree::remove(int passedValue) {
	//search first
	return true;
}